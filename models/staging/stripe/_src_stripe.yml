
sources:
  - name: stripe
    database: raw
    schema: stripe
    config:
      freshness:
        warn_after:
          count: 12
          period: hour
        error_after:
          count: 7
          period: day
      loaded_at_field: _batched_at
    tables:
      - name: payment